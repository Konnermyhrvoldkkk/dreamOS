# dreamOS

A web-based operating system inspired by Galaxy v6 webOS.

## Features

- Web-based OS interface
- Browser integration
- Customizable settings
- Report system

## Installation

1. Clone the repository
2. Install dependencies: `pnpm install`
3. Start the server: `npm run dev`

## Usage

Run `npm run dev` to start the development server with browser sync.